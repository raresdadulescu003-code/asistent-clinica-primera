# Versiuni fixate pentru deploy reproductibil.
#
# `pyproject.toml` are intervale (`>=`), bune pentru dezvoltare. În producție
# vrei exact versiunile pe care ai testat: altfel, o versiune nouă a unei
# dependențe apărută între două deploy-uri poate rupe aplicația fără ca vreo
# linie din codul tău să se fi schimbat.
#
# Regenerare după `pip install -e ".[dev]"`:
#   pip freeze --exclude-editable > requirements.txt   (apoi scoate uneltele de test)
#
# Pe Render, Build Command: pip install -r requirements.txt && pip install -e . --no-deps

annotated-doc==0.0.5
annotated-types==0.8.0
anthropic==0.120.2
anyio==4.14.2
APScheduler==3.11.3
beautifulsoup4==4.15.0
certifi==2026.7.22
click==8.4.2
distro==1.9.0
docstring_parser==0.18.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
jiter==0.16.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
sniffio==1.3.1
soupsieve==2.9.1
starlette==1.4.1
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.3
tzlocal==5.4.4
uvicorn==0.52.1
watchfiles==1.2.0
websockets==17.0.1
